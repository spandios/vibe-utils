interface:
  display_name: "Split Commit (gac)"
  short_description: "Split large changes into scoped commits"
  default_prompt: "Use $split-commit-gac to break large working-tree changes into multiple logical commits using gac."
