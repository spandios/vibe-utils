interface:
  display_name: "Commit Checkpoint (gac)"
  short_description: "Ask before commit and run preferred command"
  default_prompt: "Use $commit-checkpoint-gac to pause at commit points, ask for confirmation, and run gac with my commit message."
